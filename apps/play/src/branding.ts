/** App branding shared by the slim top bar in {@link App} and {@link Account}. */

/** The small wordmark shown at the left of the slim top bar (#32). */
export const WORDMARK = 'NES Stacking Trainer';
